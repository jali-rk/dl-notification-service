spring:
  application:
    name: notifications

  # Database Configuration
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true

  # Mail Configuration for AWS SES SMTP
  mail:
    host: ${SMTP_HOST:email-smtp.ap-south-1.amazonaws.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info
      base-path: /
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    db:
      enabled: true
    defaults:
      enabled: true

# Server Configuration
server:
  port: 8080

# Email (SES SMTP) Configuration
notifications:
  email:
    sender: ${NOTIFICATIONS_EMAIL_SENDER:sankapeeris360@gmail.com}
    region: ${NOTIFICATIONS_EMAIL_REGION:ap-south-1}
  bff:
    base-url: ${BFF_BASE_URL}

# Liquibase uses default changelog path classpath:/db/changelog/db.changelog-master.yaml
